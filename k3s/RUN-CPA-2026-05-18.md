---
type: run
area: k3s
cluster: cpa
status: completed
completed_at: 2026-05-18
---

# CPA K3s 설치 기록 — 2026-05-18

[RUNBOOK](RUNBOOK.md)의 CPA 적용 기록이다. 이 문서는 실제 값과 확인 결과만 기록한다. 인증 정보와 비밀 값은 저장하지 않는다.

## 1. 범위

CPA는 단일 control-plane K3s 클러스터다. 기본 K3s networking, kube-proxy, Traefik, local storage, Helm controller를 사용하지 않고 Cilium을 설치한다. Cilium이 정상 동작하면 이 설치 기록의 범위는 완료다.

| 항목 | 값 |
| --- | --- |
| cluster / kubectl context | cpa |
| control-plane node | cpa |
| control-plane IP | 192.168.254.4 |
| Service CIDR | 172.31.128.0/17 |
| CoreDNS Service IP | 172.31.128.10 |
| Cilium Pod CIDR | 172.31.0.0/17 |
| ServiceAccount issuer | https://oidc.k3s.ghilbut.com/cpa |
| Cilium chart | 1.19.3 |

## 2. VM과 Ubuntu

Synology Virtual Machine Manager에서 다음 값으로 VM을 생성한다.

| 항목 | 값 |
| --- | --- |
| VM 이름 | cpa |
| CPU | 4 vCPU, Hyper-V Enlightenments 사용, 예약 CPU thread 2 |
| 메모리 | 12 GB |
| 디스크 | 1,024 GB |
| 네트워크 | Default VM Network, 192.168.254.4 고정 할당 |
| OS ISO | /docker/ubuntu-24.04.3-live-server-amd64.iso |
| firmware | Legacy BIOS |
| autostart | Last state |
| 권한 | ghilbut |

Ubuntu 디스크 구성은 다음과 같다.

| 파티션 또는 경로 | 용량 | 용도 |
| --- | ---: | --- |
| BIOS GRUB spacer | 1 MiB | GRUB |
| /boot | 4 GiB | 부팅 |
| / | 128 GiB | OS |
| /home | 8 GiB | 사용자 홈 |
| /var/log | 32 GiB | 로그 |
| /var/lib/kubelet | 128 GiB | kubelet |
| /var/lib/rancher | 8 GiB | K3s 루트 |
| /var/lib/rancher/k3s/server/db | 64 GiB | embedded etcd |
| /var/lib/rancher/k3s/agent/containerd | 256 GiB | containerd |
| 미마운트 영역 | 약 396 GiB | OpenEBS LVM |

```shell
# 관리자 컴퓨터
export CLUSTER='cpa'
export SERVER_IP='192.168.254.4'
export SSH_USER='ghilbut'
export SSH_IDENTITY_FILE="$HOME/.ssh/id_ed25519"
export KUBECONFIG_PATH="$HOME/.kube/$CLUSTER.yaml"

ssh-copy-id -i "$SSH_IDENTITY_FILE.pub" "$SSH_USER@$SERVER_IP"
cat >>~/.ssh/config <<EOF
Host cpa
  HostName $SERVER_IP
  User $SSH_USER
  IdentityFile $SSH_IDENTITY_FILE
  IdentitiesOnly yes
EOF
chmod 600 ~/.ssh/config
ssh cpa

# cpa
sudo apt update -y
sudo apt full-upgrade -y
sudo apt install -y qemu-guest-agent
sudo systemctl enable --now qemu-guest-agent
sudo systemctl is-active qemu-guest-agent
lsblk -o NAME,FSTYPE,SIZE,TYPE,MOUNTPOINTS
```

관리자 kubeconfig를 원격에서 읽으려면 ghilbut 사용자가 sudo를 비밀번호 없이 실행할 수 있어야 한다. passwordless sudo를 설정하고 문법을 확인한다.

```shell
echo 'ghilbut ALL=(ALL:ALL) NOPASSWD: ALL' | sudo tee /etc/sudoers.d/ghilbut >/dev/null
sudo chmod 440 /etc/sudoers.d/ghilbut
sudo visudo -cf /etc/sudoers.d/ghilbut
```

## 3. K3s server

K3s 버전은 지정하지 않고 stable 채널로 설치했다. 이 실행에서 확인한 버전은 v1.35.4+k3s1이다.

```shell
# cpa
export CLUSTER='cpa'
export SERVER_IP='192.168.254.4'
export CLUSTER_DNS_IP='172.31.128.10'
export SERVICE_CIDR='172.31.128.0/17'
export OIDC_ISSUER='https://oidc.k3s.ghilbut.com/cpa'

export INSTALL_K3S_EXEC="\
--cluster-dns $CLUSTER_DNS_IP \
--cluster-init \
--disable local-storage \
--disable traefik \
--disable-helm-controller \
--disable-kube-proxy \
--disable-network-policy \
--flannel-backend none \
--kube-apiserver-arg service-account-issuer=$OIDC_ISSUER \
--kube-apiserver-arg service-account-jwks-uri=$OIDC_ISSUER/openid/v1/jwks \
--kube-controller-manager-arg allocate-node-cidrs=false \
--kubelet-arg allowed-unsafe-sysctls=net.ipv4.* \
--node-name=$CLUSTER \
--service-cidr $SERVICE_CIDR"

curl -sfL https://get.k3s.io | sh -
sudo systemctl is-active k3s
sudo journalctl -u k3s -n 100 --no-pager
sudo k3s --version
```

서버 kubeconfig를 관리자 컴퓨터에 등록한다. 이 파일은 인증서와 private key를 포함하므로 저장소에 넣지 않는다.

```shell
mkdir -p ~/.kube
ssh cpa 'sudo cat /etc/rancher/k3s/k3s.yaml' \
  | sed "s#https://127.0.0.1:6443#https://$SERVER_IP:6443#" \
  > "$KUBECONFIG_PATH"
chmod 600 "$KUBECONFIG_PATH"
KUBECONFIG="$KUBECONFIG_PATH" kubectl config rename-context default "$CLUSTER"
KUBECONFIG="$KUBECONFIG_PATH" kubectl get nodes
```

CNI 이전의 NotReady는 예상된다. Cilium 설치 뒤 Ready를 완료 조건으로 판단한다.

## 4. Cilium

```shell
export KUBECONFIG="$KUBECONFIG_PATH"
export CILIUM_VERSION='1.19.3'
export CILIUM_SERVICE_HOST='192.168.254.4'
export CILIUM_SERVICE_PORT='6443'
export CILIUM_POD_CIDR='172.31.0.0/17'
export CILIUM_POD_MASK_SIZE='24'
export CILIUM_OPERATOR_REPLICAS='1'

kubectl taint nodes --all node.cilium.io/agent-not-ready=true:NoExecute
kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/mcs-api/master/config/crd/multicluster.x-k8s.io_serviceexports.yaml
kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/mcs-api/master/config/crd/multicluster.x-k8s.io_serviceimports.yaml

helm repo add cilium https://helm.cilium.io/
helm repo update
helm upgrade --install cilium cilium/cilium \
  --version "$CILIUM_VERSION" \
  --namespace kube-system \
  --values /dev/stdin <<YAML
k8sServiceHost: $CILIUM_SERVICE_HOST
k8sServicePort: $CILIUM_SERVICE_PORT
ipam:
  operator:
    mode: cluster-pool
    clusterPoolIPv4MaskSize: $CILIUM_POD_MASK_SIZE
    clusterPoolIPv4PodCIDRList: $CILIUM_POD_CIDR
kubeProxyReplacement: true
l7Proxy: false
operator:
  replicas: $CILIUM_OPERATOR_REPLICAS
YAML

kubectl rollout status daemonset/cilium -n kube-system --timeout=10m
kubectl rollout status deployment/cilium-operator -n kube-system --timeout=10m
kubectl get nodes -o wide
kubectl get pods -A
```

CoreDNS 또는 metrics-server가 CNI 이전에 Pending으로 생성됐다면 재생성한다.

```shell
kubectl get pods -A \
  -o custom-columns=NAMESPACE:.metadata.namespace,NAME:.metadata.name,HOSTNETWORK:.spec.hostNetwork \
  --no-headers=true \
  | awk '$3 == "<none>" {print "-n " $1 " " $2}' \
  | xargs -r -L 1 kubectl delete pod
```

## 5. 완료 확인

```shell
kubectl --context cpa get nodes -o wide
kubectl --context cpa get pods -A
helm --kube-context cpa list -A
```

Cilium DaemonSet과 Operator가 준비되고 cpa 노드가 Ready이면 CPA 설치는 완료다. 이 실행 기록에는 agent 추가를 포함하지 않는다. ServiceAccount OIDC 메타데이터 게시, AWS IAM federation 경계와 Argo CD는 [OIDC](OIDC.md)에서 다룬다.

## 6. 실행 결과

| 확인 항목 | 결과 | 수행 시각 |
| --- | --- | --- |
| VM 및 Ubuntu 준비 | 완료 | 2026-05-10 |
| K3s server | 완료 | 2026-05-10 |
| Cilium | 완료 | 2026-05-18 |
